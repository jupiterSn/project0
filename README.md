first i created the necessary folders and repository which we already learned how to do in class, 
second i created a reusable navbar component from bootstrap and linked it to my app component using it from there, 
third i used material ui to get a reusable component (slider) and installed it too in app component using it from there,  
i added for both reusable components css styling files just so later i can change their styles if needed, 
i made sure to commit all steps and pushed them to github, 
i used an ai tool to help me through the steps and you will find the link attached to my assignment.
         changed my css filed to scss files, created a new folder in src called features that icluded a auth frature that has a login action . 
         . Added a login page but still is on the same page with navbar need to work on that,
         deleted bootstrap from my project and added tailwind.....
         Tailwind CSS is a modern, utility‑first CSS framework that lets me style my React components directly with class names instead of writing lots of custom CSS. To add it to my React + TypeScript project with Vite, i installed Tailwind, configured it in my build tool, and import it into a CSS file.
         .........Added tailwind by terminal command npm install -D tailwindcss....because my project is React + TypeScript with Vite.That method ensures Tailwind is processed during the build, so unused classes can be purged and performance stays high.The @tailwindcss/vite plugin integrates Tailwind directly into Vite’s pipeline — no manual PostCSS setup needed........
         bootstrap gives me pre‑built components (Navbar, buttons, modals) with a fixed design system. i mostly adapt what’s already there. while tailwind gives me utility classes so i design layouts and styles directly......
         modified vite.config.ts......
         modified my index.css so now Tailwind’s utilities are now available everywhere in my app...
         deleted navbar and silder....
         deleted all css files and kept just the index file that already have imported tailwind and modified files accordingly
        